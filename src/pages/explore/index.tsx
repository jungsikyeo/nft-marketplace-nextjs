import type { NextPage } from 'next';

const Explore: NextPage = ({ contract, currentAccount }) => {
  return (
    <div>
      <div>
        <main className="p-4 md:p-11">Explore</main>
      </div>
    </div>
  );
};

export default Explore;
